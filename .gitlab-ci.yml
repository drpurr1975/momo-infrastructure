---
stages:
  - terraform
  - helm

yc-terraform:
  stage: terraform
  trigger:
    include:
      - local: 'terraform/.gitlab-ci.yml'
    strategy: depend
  # rules:
  #   - changes:
  #     - frontend/**/*

# helm:
#   stage: helm
#   trigger:
#     include:
#       - local: 'momo-store-chart/.gitlab-ci.yml'
#     strategy: depend