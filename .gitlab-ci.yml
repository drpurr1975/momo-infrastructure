---
stages:
  - kubernetes

yc-terraform:
  stage: kubernetes
  trigger:
    include:
      - local: 'terraform/.gitlab-ci.yml'
    strategy: depend
  # rules:
  #   - changes:
  #     - frontend/**/*